#include <iostream>
using namespace std;

int pow (int x, int y)
{
    int pow(int base, int exp)
    int 
}

int main()
{
    int base;
    cin >> base;
    cout << "The power of the entered values is: ";
    for (int i = 0; i < base; i++)
    {
        cout << pow(i) << " ";
    }

    cout << endl;
    return 0;
}